# TODOS

- Build `DESIGN.md` as the single source of truth for typography, spacing, radius, shadows, buttons, and card hierarchy.
- Add support for system font scaling (dynamic type) and validate layout on small screens.
